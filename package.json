{
  "name": "lens-tracker",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "build": "esbuild app.js --bundle --outfile=app.bundle.js --format=esm --platform=browser",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "^2.0.0",
    "vitest": "^2.0.0"
  }
}
